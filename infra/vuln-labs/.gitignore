# Terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# SSH keys (generated by Terraform)
*-key.pem

# Plan files
*.tfplan
tfplan

# Checkov results (generated during CI)
checkov-results.json
